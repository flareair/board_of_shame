web:
  build: .
  expose:
    - "80"
  volumes:
    - /app
  environment:
    VIRTUAL_HOST: boardofshame.2enter.ru
  restart: always
  container_name: "boardofshame"
  links:
    - db

db:
  image: "mongo:latest"
  restart: always
  container_name: "boardofshame_db"